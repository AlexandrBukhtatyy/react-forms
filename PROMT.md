# Задача: Составь детальный пошаговый план рефакторинга библиотеки форм

## Роль

Ты — **Senior Frontend Architect** со специализацией в:
- TypeScript архитектуре и ООП паттернах
- Реактивном программировании (@preact/signals)
- Проектировании библиотек форм (Angular Forms, React Hook Form, Formik)
- Принципах SOLID, DDD, Clean Architecture

## Контекст проекта

**Текущее состояние**: Библиотека форм на основе @preact/signals-react с архитектурой FormNode (аналог AbstractControl из Angular).

**Ключевые файлы**:
- [architecture-analysis.md](architecture-analysis.md) — полный анализ проблем и рекомендации
- [class-diagram-clean.md](class-diagram-clean.md) — описание диаграммы классов
- [class-diagram-clean.mmd](class-diagram-clean.mmd) — Mermaid диаграмма классов
- [CLAUDE.md](CLAUDE.md) — документация проекта

**Структура библиотеки**:
```
src/lib/forms/
├── core/
│   ├── nodes/           # FormNode, FieldNode, GroupNode, ArrayNode
│   ├── contexts/        # ValidationContext, BehaviorContext
│   └── registries/      # ValidationRegistry, BehaviorRegistry (Singleton - нужно заменить)
├── validators/          # Validation Schema API
├── behaviors/           # Behavior Schema API
└── hooks/              # React хуки
```

## Цель рефакторинга

Устранить выявленные **архитектурные проблемы** согласно [architecture-analysis.md](architecture-analysis.md):

### 🔴 Критичные (Приоритет 1)
1. **God Class**: GroupNode имеет 30+ методов, смешивает 7+ ответственностей
2. **Singleton анти-паттерн**: ValidationRegistry/BehaviorRegistry — глобальное состояние, race conditions
3. **Дублирование**: 4 места парсят пути, каждый узел дублирует disable/enable/markAs*

### 🟡 Средние (Приоритет 2)
4. **Ручное кеширование**: O(n) вместо computed signals
5. **7 методов behaviors**: без абстракции (нужен Strategy паттерн)
6. **Парсинг путей**: дублируется в 4 классах

### 🟢 Желательные (Приоритет 3)
7. **ISP нарушен**: FormNode имеет optional методы
8. **Управление подписками**: просто массив функций
9. **FieldNode перегружен**: смешивает валидацию, UI, состояние

## Требования к плану

### Обязательно учесть

1. **Композиция вместо DI**:
   - ❌ НЕ использовать Dependency Injection через конструктор
   - ✅ Каждый GroupNode владеет своими экземплярами реестров
   - ✅ Пример: `private readonly validationRegistry = new ValidationRegistryClass()`

2. **Обратная совместимость**:
   - Существующий API форм должен продолжать работать
   - Legacy код (FormStore, FieldController) пока не трогаем
   - Миграция должна быть постепенной

3. **Сохранить реактивность**:
   - Все signals должны остаться reactive
   - Computed signals вместо ручных вычислений
   - effect() для side-effects

4. **Тестируемость**:
   - Изоляция форм (каждая форма = независимый экземпляр)
   - Классы < 200 строк (SRP)
   - Возможность unit-тестирования каждого класса

### Формат плана

План должен включать:

1. **Фазы рефакторинга** (3-5 фаз)
   - Каждая фаза = законченный шаг с рабочим кодом
   - Можно коммитить после каждой фазы

2. **Для каждой фазы**:
   - Цель фазы (что делаем)
   - Список задач (пошаговые действия)
   - Затрагиваемые файлы (какие файлы меняем/создаем)
   - Критерии готовности (как понять, что фаза завершена)
   - Риски и митигация (что может пойти не так)

3. **Порядок выполнения**:
   - От простого к сложному
   - Минимизация breaking changes
   - Возможность откатов (каждая фаза стабильна)

4. **Метрики успеха**:
   - Снижение сложности (GroupNode: 30 методов → 15)
   - Устранение дублирования (4 дубликата парсинга → 1)
   - Покрытие тестами (0% → 100%)
   - Размер классов (все < 200 строк)

## Ограничения

- **НЕ трогать** legacy код (src/lib/forms/core/legacy/)
- **НЕ менять** публичный API FormNode/FieldNode/GroupNode/ArrayNode
- **НЕ использовать** сторонние библиотеки (только @preact/signals, immer)
- **НЕ ломать** существующие примеры (src/examples/)

## Ожидаемый результат

Детальный **пошаговый план** в формате:

```markdown
# План рефакторинга библиотеки форм

## Обзор
[Краткое описание стратегии]

## Фаза 1: [Название]
**Цель**: ...
**Задачи**:
1. ...
2. ...
**Файлы**: ...
**Критерии готовности**: ...
**Риски**: ...

## Фаза 2: ...
[и так далее]

## Итоговые метрики
[Ожидаемые улучшения]
```

---

**Важно**: План должен быть **практичным** и **реализуемым**. Учти, что это реальный production код, используемый в проектах.