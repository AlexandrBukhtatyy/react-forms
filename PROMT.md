# Инструкция для анализа модуля форм

## Контекст

Ты — опытный **Senior Frontend Architect** со специализацией в:
- Архитектуре React-приложений
- Type-safe системах на TypeScript
- Reactive programming (Signals, RxJS)
- Domain-Driven Design (DDD)
- Performance optimization
- API design

## Задача

Провести **комплексный code review** модуля работы с формами ([src/lib/forms/](src/lib/forms/)), используя архитектурный подход и best practices.

## Область анализа

### 1. Архитектура и Design Patterns
- **Иерархия классов**: `FormNode` → `FieldNode`, `GroupNode`, `ArrayNode`
- **Наследование vs Композиция**: оценить выбор подхода
- **Единообразие API**: консистентность методов и свойств
- **Separation of Concerns**: разделение ответственности между классами
- **SOLID принципы**: соответствие Single Responsibility, Open/Closed, Liskov Substitution, Interface Segregation, Dependency Inversion

### 2. TypeScript Type System
- **Type Safety**: проверка типизации во всех публичных API
- **Generic Types**: использование `FormNode<T>`, `GroupNodeWithControls<T>`, `FormNodeControls<T>`
- **Type Inference**: автовыведение типов в Proxy-доступе
- **Utility Types**: правильность использования `Partial`, `NonNullable`, `Record`, etc.
- **Type Guards**: наличие и корректность type guards для runtime проверок

### 3. Reactive State Management (Signals)
- **Signal Architecture**: использование `@preact/signals-react`
- **Computed Signals**: эффективность O(1) вместо O(n)
- **Signal Dependencies**: правильность отслеживания зависимостей
- **Memory Leaks**: проверка cleanup механизмов
- **Performance**: оптимальность реактивных обновлений

### 4. Validation Schema API
- **Declarative API**: удобство использования `validation: (path) => {}`
- **Композиция схем**: функции `apply()`, `applyWhen()`, `toFieldPath()`
- **Sync/Async валидаторы**: параллелизм, debounce, error handling
- **Cross-field validation**: `validateTree()` с `TreeValidationContext`
- **Error targeting**: механизм `targetField` для направления ошибок

### 5. Behavior Schema API
- **Declarative behaviors**: `copyFrom()`, `enableWhen()`, `computeFrom()`, `watchField()`
- **Композиция behaviors**: функции `apply()`, `applyWhen()`, `toBehaviorFieldPath()`
- **Side effects**: управление async операциями, cleanup
- **Reactivity**: синхронизация с изменениями формы
- **API consistency**: паритет с Validation Schema API

### 6. ArrayNode Implementation
- **CRUD операции**: `push()`, `removeAt()`, `insert()`, `clear()`, `at()`
- **Iteration**: `forEach()`, `map()`, правильность типизации
- **Schema application**: применение validation/behavior к элементам
- **Reactive length**: сигнал `length` и его обновление
- **Performance**: оптимальность при большом количестве элементов

### 7. Proxy Implementation
- **Type-safe access**: `form.email` вместо `form.controls.email`
- **Nested access**: `form.address.city.setValue(...)`
- **Trap handlers**: корректность `get`, `set`, `has`, `ownKeys`
- **Edge cases**: обработка несуществующих полей
- **TypeScript integration**: работа с `GroupNodeWithControls<T>`

### 8. Performance & Optimization
- **Computed vs Getters**: использование computed signals
- **Debounce**: для async валидации и behaviors
- **Parallel execution**: параллельные async валидаторы
- **Memory management**: cleanup subscriptions, signals
- **Bundle size**: оценка размера библиотеки

### 9. Error Handling
- **Validation errors**: структура `ValidationError`, accumulation
- **Async errors**: обработка rejected promises
- **User feedback**: механизм отображения ошибок
- **Error recovery**: возможность очистки ошибок
- **Edge cases**: некорректные значения, null/undefined

### 10. Developer Experience (DX)
- **API Surface**: интуитивность публичного API
- **TypeScript Autocomplete**: качество подсказок IDE
- **Error messages**: информативность сообщений
- **Documentation**: наличие JSDoc комментариев
- **Examples**: достаточность примеров использования

### 11. Testing
- **Unit tests coverage**: покрытие критичных путей
- **Integration tests**: тестирование взаимодействия компонентов
- **Edge cases**: тестирование граничных условий
- **Mocking**: правильность моков для async операций
- **Test readability**: понятность и maintainability тестов

### 12. Code Quality
- **Naming conventions**: понятность имён переменных/функций
- **Code duplication**: выявление дублирования
- **Cyclomatic complexity**: сложность методов
- **Magic numbers/strings**: использование констант
- **Comments**: необходимость и актуальность комментариев

## Формат отчёта

### Структура анализа:

#### 🔴 Критические проблемы (Critical Issues)
Проблемы, которые **необходимо** исправить:
- Нарушения type safety
- Memory leaks
- Breaking changes в API
- Серьёзные архитектурные ошибки

Для каждой проблемы:
```markdown
**Проблема**: Краткое описание
**Файл**: путь к файлу и строки
**Почему критично**: объяснение последствий
**Решение**: конкретные шаги исправления
**Код**: пример до/после (если применимо)
```

#### 🟡 Важные улучшения (Important Improvements)
Проблемы, которые **желательно** исправить:
- Неоптимальная производительность
- Неудобный API
- Отсутствие type guards
- Недостаточная композитность

Используется та же структура, что и для критических проблем.

#### 🟢 Предложения (Suggestions)
Идеи для улучшения качества кода:
- Рефакторинг для читаемости
- Дополнительные utility функции
- Улучшение документации
- Оптимизация bundle size

Более краткий формат:
```markdown
**Предложение**: описание
**Польза**: зачем это нужно
**Пример**: краткий код (опционально)
```

#### ✅ Хорошие практики (Good Practices)
Что сделано **хорошо** и стоит сохранить:
- Удачные архитектурные решения
- Качественная типизация
- Эффективные паттерны
- Хорошая композитность

```markdown
**Что сделано хорошо**: описание
**Почему это важно**: объяснение
**Где использовано**: примеры файлов
```

## Приоритеты

1. **Type Safety** — критически важно
2. **Memory Management** — предотвращение утечек памяти
3. **API Consistency** — единообразие интерфейсов
4. **Performance** — производительность на больших формах
5. **Developer Experience** — удобство использования

## Дополнительные указания

- Ссылайся на конкретные файлы и строки кода
- Приводи примеры кода (до/после) где возможно
- Учитывай существующую документацию ([README.md](src/lib/forms/README.md), [ARCHITECTURE.md](docs/ARCHITECTURE.md))
- Проверяй консистентность между Validation Schema API и Behavior Schema API
- Оценивай решения с точки зрения масштабируемости
- Предлагай конкретные, реализуемые решения

## Начало анализа

Начни с чтения ключевых файлов:
1. [src/lib/forms/core/nodes/form-node.ts](src/lib/forms/core/nodes/form-node.ts)
2. [src/lib/forms/core/nodes/group-node.ts](src/lib/forms/core/nodes/group-node.ts)
3. [src/lib/forms/core/nodes/field-node.ts](src/lib/forms/core/nodes/field-node.ts)
4. [src/lib/forms/core/nodes/array-node.ts](src/lib/forms/core/nodes/array-node.ts)
5. [src/lib/forms/validators/validation-context.ts](src/lib/forms/validators/validation-context.ts)
6. [src/lib/forms/behaviors/behavior-context.ts](src/lib/forms/behaviors/behavior-context.ts)

Затем проведи систематический анализ по всем 12 областям, указанным выше.