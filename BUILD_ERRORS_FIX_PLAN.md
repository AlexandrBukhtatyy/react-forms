# 📋 План исправления ошибок сборки

> **Статус**: В работе
> **Всего ошибок**: 57 в библиотеке + 35 в тестах
> **Время**: 4-6 часов
> **Дата создания**: 2025-01-09

---

## 📊 Текущее состояние

### ✅ Исправлено (критичные ошибки)
- [x] Перегрузка `apply()` для form-level композиции (6 ошибок)
- [x] Типизация `BehaviorContext` с `extends Record<string, any>` (5 ошибок)
- [x] Компонент `Select` - исправление ref (2 ошибки)
- [x] Компонент `InputSearch` - resource и useRef (2 ошибки)
- [x] Неявные типы `any` в коде приложения (3 ошибки)
- [x] Неиспользуемые переменные в приложении (3 ошибки)

### ⏳ Осталось исправить
- [ ] **Фаза 1**: Generic constraints (17 ошибок TS2344)
- [ ] **Фаза 2**: SubscriptionManager (8 ошибок TS2554 + TS2339)
- [ ] **Фаза 3**: Неиспользуемые переменные в библиотеке (12 ошибок TS6133)
- [ ] **Фаза 4**: Проблемы с типами свойств (5 ошибок TS2339)
- [ ] **Фаза 5**: Несовместимость типов (8 ошибок)
- [ ] **Фаза 6**: Отсутствующие модули (4 ошибки TS2307)
- [ ] **Фаза 7**: Остальные ошибки (9 ошибок)

---

## 🎯 Фаза 1: Generic Constraints (17 ошибок TS2344)

**Проблема**: После добавления `extends Record<string, any>` в `BehaviorContext`, все зависимые типы требуют такого же ограничения.

**Время**: 1-1.5 часа

### Шаг 1.1: behavior-factories.ts

**Файл**: `src/lib/forms/core/behaviors/behavior-factories.ts`

**Что делать**:
```typescript
// Найти все функции create*Behavior и добавить constraint

// ❌ Было
export function createCopyFromBehavior<T, TSource>(...)

// ✅ Стало
export function createCopyFromBehavior<T extends Record<string, any>, TSource>(...)
```

**Функции для исправления** (~строки):
- [ ] `createCopyFromBehavior` (строка ~31)
- [ ] `createEnableWhenBehavior` (строка ~149)
- [ ] `createDisableWhenBehavior`
- [ ] `createComputeFromBehavior` (строка ~229)
- [ ] `createWatchFieldBehavior`
- [ ] `createRevalidateWhenBehavior`
- [ ] `createSyncFieldsBehavior`

**Проверка**:
```bash
npm run build 2>&1 | grep "behavior-factories.ts" | grep "TS2344"
# Должно быть: 0 ошибок
```

---

### Шаг 1.2: behavior-registry.ts

**Файл**: `src/lib/forms/core/behaviors/behavior-registry.ts`

**Что делать**:
```typescript
// ❌ Было (строка ~128)
registerBehavior<T>(
  form: GroupNode<T>,
  ...
): void

// ✅ Стало
registerBehavior<T extends Record<string, any>>(
  form: GroupNode<T>,
  ...
): void
```

**Методы для исправления**:
- [ ] `registerBehavior` (строка ~128)
- [ ] `applyBehaviorSchema` (строка ~169)
- [ ] Другие публичные методы класса

**Проверка**:
```bash
npm run build 2>&1 | grep "behavior-registry.ts" | grep "TS2344"
# Должно быть: 0 ошибок
```

---

### Шаг 1.3: types.ts

**Файл**: `src/lib/forms/core/behaviors/types.ts`

**Что делать**:
```typescript
// ❌ Было (строка ~116)
export type BehaviorHandlerFn<TForm = any> = (
  form: import('../nodes/group-node').GroupNode<TForm>,
  context: BehaviorContext<TForm>,
  ...
) => (() => void) | null;

// ✅ Стало
export type BehaviorHandlerFn<TForm extends Record<string, any> = any> = (
  form: import('../nodes/group-node').GroupNode<TForm>,
  context: BehaviorContext<TForm>,
  ...
) => (() => void) | null;
```

**Типы для исправления**:
- [ ] `BehaviorHandlerFn` (строка ~117)

**Проверка**:
```bash
npm run build 2>&1 | grep "behavior.*types.ts" | grep "TS2344"
# Должно быть: 0 ошибок
```

---

### Шаг 1.4: schema-behaviors.ts

**Файл**: `src/lib/forms/core/behaviors/schema-behaviors.ts`

**Что делать**: Найти все функции и добавить constraint `extends Record<string, any>`

**Функции для исправления**:
- [ ] Все экспортируемые функции behavior схем

---

### Шаг 1.5: array-node.ts

**Файл**: `src/lib/forms/core/nodes/array-node.ts`

**Опция А** (простая - рекомендуется):
```typescript
// Добавить constraint на уровне класса
// ❌ Было
export class ArrayNode<T> extends FormNode<T[]>

// ✅ Стало
export class ArrayNode<T extends object> extends FormNode<T[]>
```

**Опция Б** (сложная):
```typescript
// Изменить сигнатуры методов
private applyBehaviorSchema(
  behaviorSchema: BehaviorSchemaFn<T extends object ? T : never>
): void
```

**Задачи**:
- [ ] Выбрать подход (рекомендуется Опция А)
- [ ] Применить изменения
- [ ] Найти все `new ArrayNode<...>` и проверить совместимость

**Проверка**:
```bash
npm run build 2>&1 | grep "array-node.ts" | grep "TS2344"
# Должно быть: 0 ошибок
```

---

### Финальная проверка Фазы 1

```bash
npm run build 2>&1 | grep "TS2344" | grep -v "test\." | wc -l
# Ожидается: 0 ошибок
```

**Чек-лист**:
- [ ] Все функции в `behavior-factories.ts` обновлены
- [ ] Все методы в `behavior-registry.ts` обновлены
- [ ] `BehaviorHandlerFn` тип обновлён
- [ ] `ArrayNode` constraint добавлен
- [ ] Сборка проходит без TS2344

---

## 🔧 Фаза 2: SubscriptionManager (8 ошибок)

**Проблема**:
1. Вызовы `addSubscription()` ожидают 2 аргумента, передаётся 1 (6 ошибок TS2554)
2. Метод `dispose()` не существует (2 ошибки TS2339)

**Время**: 1 час

### Шаг 2.1: Найти SubscriptionManager

```bash
# Поиск файла
find src/lib/forms -name "*subscription*" -type f

# Прочитать файл и понять API
cat [путь к файлу]
```

**Задачи**:
- [ ] Найден файл SubscriptionManager
- [ ] Изучена текущая сигнатура `addSubscription()`
- [ ] Проверено наличие метода `dispose()` или аналога

---

### Шаг 2.2: Исправить API SubscriptionManager

**Файл**: `src/lib/forms/core/subscription-manager.ts` (предположительно)

**Вариант А** - добавить перегрузку (рекомендуется):
```typescript
export class SubscriptionManager {
  private subscriptions = new Map<string, () => void>();
  private counter = 0;

  // ✅ Новая перегрузка - без ключа
  addSubscription(cleanup: () => void): void;

  // ✅ Старая сигнатура - с ключом
  addSubscription(key: string, cleanup: () => void): void;

  // Реализация
  addSubscription(keyOrCleanup: string | (() => void), cleanup?: () => void): void {
    if (typeof keyOrCleanup === 'function') {
      // Генерируем уникальный ключ
      const key = `sub_${this.counter++}`;
      this.subscriptions.set(key, keyOrCleanup);
    } else {
      this.subscriptions.set(keyOrCleanup, cleanup!);
    }
  }

  // ✅ Добавить метод dispose()
  dispose(): void {
    this.subscriptions.forEach(cleanup => cleanup());
    this.subscriptions.clear();
  }
}
```

**Вариант Б** - изменить вызовы (если API нельзя менять):
```typescript
// Найти все вызовы и добавить ключи
this.subscriptions.addSubscription('valueChange', cleanup);
this.subscriptions.addSubscription('asyncValidation', cleanup);
```

**Задачи**:
- [ ] Выбрать подход
- [ ] Реализовать изменения в SubscriptionManager
- [ ] Добавить метод `dispose()` если отсутствует

---

### Шаг 2.3: Исправить вызовы в field-node.ts

**Файл**: `src/lib/forms/core/nodes/field-node.ts`

**Места для исправления**:
- [ ] Строка ~524: `this.subscriptions.addSubscription(cleanup)`
- [ ] Строка ~565: `this.subscriptions.addSubscription(cleanup)`
- [ ] Строка ~583: `this.subscriptions.dispose()`

**Если выбран Вариант А** - ничего не делать
**Если выбран Вариант Б** - добавить ключи:
```typescript
this.subscriptions.addSubscription('valueChange', cleanup);
```

---

### Шаг 2.4: Исправить вызовы в array-node.ts

**Файл**: `src/lib/forms/core/nodes/array-node.ts`

**Места для исправления**:
- [ ] Строка ~513: `this.subscriptions.addSubscription(cleanup)`
- [ ] Строка ~545: `this.subscriptions.addSubscription(cleanup)`
- [ ] Строка ~563: `this.subscriptions.dispose()`

---

### Финальная проверка Фазы 2

```bash
npm run build 2>&1 | grep -E "TS2554|TS2339" | grep -E "subscription|addSubscription|dispose" | wc -l
# Ожидается: 0 ошибок
```

**Чек-лист**:
- [ ] SubscriptionManager найден и изучен
- [ ] Метод `dispose()` добавлен
- [ ] API `addSubscription()` исправлен (перегрузка или изменение вызовов)
- [ ] Все вызовы в `field-node.ts` исправлены (3 места)
- [ ] Все вызовы в `array-node.ts` исправлены (3 места)
- [ ] Сборка проходит без ошибок подписок

---

## 🧹 Фаза 3: Неиспользуемые переменные (12 ошибок TS6133)

**Время**: 30 минут

### Шаг 3.1: behavior-factories.ts

**Файл**: `src/lib/forms/core/behaviors/behavior-factories.ts`

#### Ошибка 1: Строка ~81 - forEach на неправильном типе

```typescript
// ❌ Было
if (fields !== 'all') {
  fields.forEach((key) => {  // Ошибка: forEach не существует
    // ...
  });
}

// ✅ Стало
if (fields !== 'all') {
  const fieldArray = Array.isArray(fields) ? fields : [fields];
  fieldArray.forEach((key) => {
    // ...
  });
}
```

**Задачи**:
- [ ] Исправить строку ~81

---

#### Ошибка 2: Строка ~149-150 - Неиспользуемые переменные

```typescript
// ❌ Было
const {
  field,        // ← Не используется
  condition,    // ← Не используется
  resetOnDisable,
  debounce,
} = normalized;

// ✅ Вариант А - добавить префикс _
const {
  field: _field,
  condition: _condition,
  resetOnDisable,
  debounce,
} = normalized;

// ✅ Вариант Б - удалить совсем
const { resetOnDisable, debounce } = normalized;
```

**Задачи**:
- [ ] Исправить строку ~149-150

---

#### Ошибка 3: Строка ~269 - Неиспользуемая переменная options

```typescript
// ❌ Было
const options = normalized;
// ... options не используется далее

// ✅ Стало - удалить строку
```

**Задачи**:
- [ ] Исправить строку ~269

---

### Шаг 3.2: group-node.ts

**Файл**: `src/lib/forms/core/nodes/group-node.ts`

#### Ошибка 1: Строка ~362 - results не используется

```typescript
// ❌ Было
const results = await Promise.all(
  Array.from(this.fields.values()).map(field => field.validate())
);
// ... results не используется

// ✅ Стало
await Promise.all(
  Array.from(this.fields.values()).map(field => field.validate())
);
```

**Задачи**:
- [ ] Исправить строку ~362

---

#### Ошибка 2: Строка ~748 - restItems не используется

```typescript
// ❌ Было
const [firstItem, ...restItems] = items;

// ✅ Стало
const [firstItem] = items;
// ИЛИ если нужно для деструктуризации:
const [firstItem, ..._restItems] = items;
```

**Задачи**:
- [ ] Исправить строку ~748

---

### Финальная проверка Фазы 3

```bash
npm run build 2>&1 | grep "TS6133" | grep -v "test\." | wc -l
# Ожидается: 0 ошибок
```

**Чек-лист**:
- [ ] `behavior-factories.ts` - исправлено 3 места
- [ ] `group-node.ts` - исправлено 2 места
- [ ] Сборка проходит без TS6133 (кроме тестов)

---

## 🔍 Фаза 4: Проблемы с типами свойств (5 ошибок TS2339)

**Время**: 45 минут

### Шаг 4.1: behavior-factories.ts строка ~81

**Проблема**: `forEach` не существует на типе `keyof TSource[]`

```typescript
// Контекст (строка ~75-85)
const fields = options.fields || 'all';

if (fields !== 'all') {
  // fields имеет тип: keyof TSource[] | 'all'
  fields.forEach((key) => {  // ❌ Ошибка
    // ...
  });
}

// ✅ Решение
if (fields !== 'all') {
  // Приводим к массиву
  const fieldArray = (Array.isArray(fields) ? fields : [fields]) as (keyof TSource)[];
  fieldArray.forEach((key) => {
    // ...
  });
}
```

**Задачи**:
- [ ] Найти строку ~81
- [ ] Добавить приведение типа к массиву
- [ ] Проверить сборку

---

### Шаг 4.2: array-node.ts строка ~503

**Проблема**: `Property 'fields' is private and only accessible within class 'GroupNode'`

```typescript
// Контекст
const groupNode = new GroupNode(itemSchema);
const fields = groupNode.fields;  // ❌ Ошибка: fields - private
```

**Решение А** - добавить публичный геттер в GroupNode (рекомендуется):

**Файл**: `src/lib/forms/core/nodes/group-node.ts`
```typescript
/**
 * Получить Map полей
 * @returns Map<keyof T, FormNode<any>>
 */
public getFields(): Map<keyof T, FormNode<any>> {
  return this.fields;
}
```

**Затем в array-node.ts**:
```typescript
const fields = groupNode.getFields();
```

**Решение Б** - использовать итератор:
```typescript
// Если GroupNode реализует Symbol.iterator
const fields = new Map(Array.from(groupNode));
```

**Задачи**:
- [ ] Выбрать решение
- [ ] Реализовать в `group-node.ts`
- [ ] Обновить вызов в `array-node.ts:503`

---

### Финальная проверка Фазы 4

```bash
npm run build 2>&1 | grep "TS2339" | grep -v "test\." | wc -l
# Ожидается: 0 ошибок
```

**Чек-лист**:
- [ ] `behavior-factories.ts:81` - исправлено
- [ ] `array-node.ts:503` - исправлено
- [ ] Сборка проходит без TS2339

---

## 🔀 Фаза 5: Несовместимость типов (8 ошибок)

**Время**: 1-1.5 часа

### Шаг 5.1: behavior-registry.ts строка ~141

**Проблема**: `GroupNode<T>` не совместим с `GroupNode<Record<string, any>>`

```typescript
// ❌ Было
registerBehavior<T>(
  form: GroupNode<T>,
  ...
): void {
  const context = new BehaviorContextImpl(form);  // ❌ Ошибка
}

// ✅ Стало
registerBehavior<T extends Record<string, any>>(
  form: GroupNode<T>,
  ...
): void {
  const context = new BehaviorContextImpl(form);  // ✅ OK
}
```

**Задачи**:
- [ ] Добавить constraint `extends Record<string, any>` к generic параметру
- [ ] Проверить сборку

---

### Шаг 5.2: field-node.ts строки ~94, ~102

**Проблема**: `T | null` не совместимо с `T`

```typescript
// Контекст
class FieldNode<T> {
  private _initialValue: T;  // ← Тип T

  constructor(config: FieldConfig<T>) {
    const initialValue = config.value ?? null;  // Тип: T | null
    this._initialValue = initialValue;  // ❌ Ошибка: T | null → T
  }
}
```

**Решение А** - изменить тип поля (рекомендуется):
```typescript
private _initialValue: T | null;
```

**Решение Б** - использовать assertion:
```typescript
this._initialValue = initialValue as T;
```

**Решение В** - проверка null:
```typescript
this._initialValue = initialValue ?? (undefined as T);
```

**Задачи**:
- [ ] Выбрать решение
- [ ] Исправить строку ~94
- [ ] Исправить строку ~102 (аналогично)
- [ ] Проверить влияние на остальной код

---

### Шаг 5.3: array-node.ts строка ~212 (TS2416)

**Проблема**: Несовместимость сигнатуры `patchValue` между `ArrayNode` и базовым `FormNode`

```typescript
// FormNode базовый класс ожидает:
patchValue(value: (T | undefined)[]): void

// ArrayNode реализует:
patchValue(values: Partial<T>[]): void  // ❌ Несовместимо
```

**Решение** - привести к единой сигнатуре:
```typescript
// В ArrayNode
override patchValue(values: (Partial<T> | undefined)[]): void {
  values.forEach((value, index) => {
    if (value !== undefined) {
      const node = this.at(index);
      if (node) {
        node.patchValue(value);
      }
    }
  });
}
```

**Задачи**:
- [ ] Изменить сигнатуру `patchValue` в `array-node.ts:212`
- [ ] Добавить проверку на `undefined`
- [ ] Убедиться что используется `override` keyword
- [ ] Проверить сборку

---

### Остальные ошибки TS2345, TS2322

**Подход**: Найти каждую ошибку индивидуально и исправить

```bash
# Найти все оставшиеся ошибки типизации
npm run build 2>&1 | grep -E "TS2345|TS2322" | grep -v "test\."
```

**Задачи**:
- [ ] Проанализировать каждую ошибку
- [ ] Применить правильное приведение типов или исправить сигнатуру

---

### Финальная проверка Фазы 5

```bash
npm run build 2>&1 | grep -E "TS2345|TS2322|TS2416" | grep -v "test\." | wc -l
# Ожидается: 0 ошибок
```

**Чек-лист**:
- [ ] `behavior-registry.ts:141` - добавлен constraint
- [ ] `field-node.ts:94,102` - исправлен тип `_initialValue`
- [ ] `array-node.ts:212` - исправлена сигнатура `patchValue`
- [ ] Все остальные TS2345, TS2322 исправлены

---

## 📦 Фаза 6: Отсутствующие модули (4 ошибки TS2307)

**Время**: 30 минут

### Шаг 6.1: Найти все ошибки импортов

```bash
npm run build 2>&1 | grep "TS2307"
```

**Ожидаемый вывод**:
```
src/path/to/file.ts(X,Y): error TS2307: Cannot find module './missing-module'
```

**Задачи для каждой ошибки**:
- [ ] Найти строку с импортом
- [ ] Проверить существует ли файл
- [ ] Исправить путь ИЛИ создать отсутствующий файл

---

### Шаг 6.2: Типичные решения

**Проблема 1**: Неправильный путь
```typescript
// ❌ Было
import { Something } from './wrong-path';

// ✅ Стало
import { Something } from '../correct-path';
```

**Проблема 2**: Отсутствующее расширение
```typescript
// ❌ Было
import { Something } from './file';

// ✅ Стало (если нужно явное расширение)
import { Something } from './file.ts';
```

**Проблема 3**: Файл не существует - создать
```typescript
// Создать файл src/lib/forms/core/missing-module.ts
export const Something = ...;
```

---

### Финальная проверка Фазы 6

```bash
npm run build 2>&1 | grep "TS2307" | wc -l
# Ожидается: 0 ошибок
```

**Чек-лист**:
- [ ] Все 4 ошибки TS2307 найдены
- [ ] Каждая ошибка проанализирована
- [ ] Пути исправлены ИЛИ файлы созданы
- [ ] Сборка проходит без TS2307

---

## 🔧 Фаза 7: Остальные ошибки (9 ошибок)

**Время**: 1 час

### Шаг 7.1: Инвентаризация

```bash
# Получить список всех оставшихся типов ошибок
npm run build 2>&1 | grep "error TS" | grep -v "test\." | sed 's/.*error TS\([0-9]*\):.*/\1/' | sort | uniq -c
```

**Задачи**:
- [ ] Составить список уникальных кодов ошибок
- [ ] Для каждого кода найти примеры

---

### Шаг 7.2: Исправление по категориям

#### TS2341 - Private property access
```typescript
// Решение: Добавить public getter/setter
// ИЛИ изменить модификатор на protected
```

#### TS2430 - Interface incorrectly extends
```typescript
// Решение: Проверить совместимость типов в extends
```

#### TS2353 - Object literal may only specify known properties
```typescript
// Решение: Убедиться что все свойства объявлены в интерфейсе
```

#### TS2551 - Property does not exist
```typescript
// Решение: Добавить свойство в интерфейс ИЛИ исправить имя
```

**Задачи**:
- [ ] Для каждого типа ошибки найти все вхождения
- [ ] Применить соответствующее решение
- [ ] Проверить сборку после каждого исправления

---

### Финальная проверка Фазы 7

```bash
npm run build 2>&1 | grep "error TS" | grep -v "test\." | wc -l
# Ожидается: 0 ошибок
```

**Чек-лист**:
- [ ] Все уникальные типы ошибок идентифицированы
- [ ] Каждый тип ошибок исправлен
- [ ] Сборка проходит без ошибок (кроме тестов)

---

## ✅ Финальная проверка и тестирование

**Время**: 30-45 минут

### Шаг 1: Полная сборка

```bash
npm run build
```

**Ожидаемый результат**:
- ✅ 0 ошибок в `src/lib/forms`
- ✅ 0 ошибок в `src/domains`
- ⚠️ Могут остаться ошибки в `src/tests` (допустимо)

**Задачи**:
- [ ] Сборка проходит успешно
- [ ] Нет ошибок в продакшн коде

---

### Шаг 2: Линтер

```bash
npm run lint
```

**Задачи**:
- [ ] Линтер проходит без ошибок
- [ ] Исправлены все предупреждения (optional)

---

### Шаг 3: Запуск приложения

```bash
npm run dev
```

**Проверить**:
- [ ] Приложение запускается
- [ ] Нет ошибок в консоли браузера
- [ ] Базовая функциональность работает

---

### Шаг 4: Функциональное тестирование

**Проверить основные сценарии**:

#### Создание формы
```typescript
const form = new GroupNode({
  email: { value: '', component: Input },
  password: { value: '', component: Input }
});
```
- [ ] Форма создаётся без ошибок

#### Валидация
```typescript
form.applyValidationSchema((path) => {
  required(path.email);
  email(path.email);
});

await form.validate();
```
- [ ] Валидация работает
- [ ] Ошибки отображаются корректно

#### Behaviors
```typescript
form.applyBehaviorSchema((path) => {
  copyFrom(path.field1, path.field2);
});
```
- [ ] Behaviors применяются
- [ ] Реактивность работает

#### Submit
```typescript
await form.submit(async (values) => {
  console.log(values);
});
```
- [ ] Submit работает
- [ ] Данные передаются корректно

---

### Шаг 5: Тесты (опционально)

```bash
npm test
```

**Задачи**:
- [ ] Запустить тесты
- [ ] Проанализировать падающие тесты
- [ ] Исправить критичные (optional)

---

## 📝 Коммит и документация

### Шаг 1: Создать коммит

```bash
git add .
git commit -m "fix: исправлены ошибки компиляции библиотеки форм

Исправлено 57 ошибок TypeScript:
- TS2344: Добавлены generic constraints для BehaviorContext (17 ошибок)
- TS2554/TS2339: Исправлен API SubscriptionManager (8 ошибок)
- TS6133: Удалены неиспользуемые переменные (12 ошибок)
- TS2339: Исправлен доступ к приватным свойствам (5 ошибок)
- TS2345/TS2322/TS2416: Исправлена совместимость типов (8 ошибок)
- TS2307: Исправлены импорты (4 ошибки)
- Остальные типизационные ошибки (3 ошибки)

Изменённые файлы:
- src/lib/forms/core/behaviors/behavior-factories.ts
- src/lib/forms/core/behaviors/behavior-registry.ts
- src/lib/forms/core/behaviors/types.ts
- src/lib/forms/core/behaviors/schema-behaviors.ts
- src/lib/forms/core/nodes/array-node.ts
- src/lib/forms/core/nodes/field-node.ts
- src/lib/forms/core/nodes/group-node.ts
- src/lib/forms/core/subscription-manager.ts

Все критичные ошибки устранены. Приложение готово к разработке.

Refs: #[номер issue если есть]"
```

---

### Шаг 2: Обновить документацию (optional)

**Файлы для обновления**:
- [ ] `README.md` - если изменился API
- [ ] `CHANGELOG.md` - добавить запись о исправлениях
- [ ] `TODO.md` - отметить завершённые задачи

---

## 📊 Отслеживание прогресса

### Общий прогресс

- [ ] **Фаза 1**: Generic constraints (17 ошибок) - 0/17
- [ ] **Фаза 2**: SubscriptionManager (8 ошибок) - 0/8
- [ ] **Фаза 3**: Неиспользуемые переменные (12 ошибок) - 0/12
- [ ] **Фаза 4**: Типы свойств (5 ошибок) - 0/5
- [ ] **Фаза 5**: Несовместимость типов (8 ошибок) - 0/8
- [ ] **Фаза 6**: Отсутствующие модули (4 ошибки) - 0/4
- [ ] **Фаза 7**: Остальные (9 ошибок) - 0/9

**Всего**: 0/63 ошибок исправлено

---

### Время выполнения (план vs факт)

| Фаза | План | Факт | Статус |
|------|------|------|--------|
| Фаза 1 | 1-1.5ч | - | ⏳ |
| Фаза 2 | 1ч | - | ⏳ |
| Фаза 3 | 0.5ч | - | ⏳ |
| Фаза 4 | 0.75ч | - | ⏳ |
| Фаза 5 | 1-1.5ч | - | ⏳ |
| Фаза 6 | 0.5ч | - | ⏳ |
| Фаза 7 | 1ч | - | ⏳ |
| Тестирование | 0.5-0.75ч | - | ⏳ |
| **Итого** | **4-6ч** | **-** | ⏳ |

---

## 🚨 Альтернативный подход (быстрый)

Если нужно срочно разблокировать разработку:

### Минимальный набор (1-2 часа)

1. **Только блокирующие ошибки**:
   - [ ] TS2307 - отсутствующие модули (блокирует импорты)
   - [ ] TS2554 - SubscriptionManager (блокирует создание форм)

2. **Временные решения**:
   ```typescript
   // @ts-expect-error - TODO: fix generic constraints
   const context = new BehaviorContextImpl(form);
   ```

3. **Создать GitHub Issues** для остальных проблем

4. **Продолжить разработку** и вернуться позже

---

## 📞 Помощь и вопросы

**При возникновении проблем**:

1. Проверить текущие ошибки:
   ```bash
   npm run build 2>&1 > current-errors.log
   ```

2. Сравнить с предыдущим состоянием:
   ```bash
   diff errors.log current-errors.log
   ```

3. Откатить изменения если что-то сломалось:
   ```bash
   git checkout -- [файл]
   ```

4. Создать issue с описанием проблемы

---

**Удачи в исправлении! 🚀**
